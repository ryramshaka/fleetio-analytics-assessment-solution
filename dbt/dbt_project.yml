name: fleetio_analytics

profile: fleetio_profile

version: 0.0.1
dbt-cloud:
  project-id: 70471823569643

seed-paths: ["seeds"]
model-paths: ["models"]
macro-paths: ["macros"]
clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  # Builds seeds into '<your_schema_name>_raw'
  fleetio_analytics:
    +schema: raw

models:
  fleetio_analytics:
    # Materialize staging models as views, and marts as tables
    staging:
      +materialized: view
    marts:
      +materialized: table
